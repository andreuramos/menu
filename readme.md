# Menu & Groceries generator

PHP API + vue/react UI + mysql DB

API mvp
* create recipe (name, category)
* generate weekly menu with random recipes
* store the generated menu
* replace recipe for a given day
